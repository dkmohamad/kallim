.env
__pycache__/
*.pyc
*.egg-info/
.venv/
pending/
audio/
output/
scratch/

# Lesson transcripts: private content — the teacher is named throughout and the
# pages link a private Notion workspace. This repo is public, so they stay local.
transcripts/

# Script audio cache — kept out of audio/ so `prune` never sees it.
audio-scripts/
